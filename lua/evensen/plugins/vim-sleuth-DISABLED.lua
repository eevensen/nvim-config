-- Detect tabstop and shiftwidth automatically
-- Adjusts indentation settings
-- (like shiftwidth and expandtab) based on
-- the current file and surrounding files.
return {
  -- 'tpope/vim-sleuth',
}
